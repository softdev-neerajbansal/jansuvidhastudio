# Copy to .env for local development (vite dev only — never commit .env).
# In production, set this as a real server environment variable for PHP,
# or create public/api/config.local.php on the server (see chat.php) —
# never commit the real key to this file or to git.
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=AIza...
